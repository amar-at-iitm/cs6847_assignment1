apiVersion: v1
kind: Service
metadata:
  name: flask-app
spec:
  selector:
    app: flask-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000       # service port
      targetPort: 5000 # container port
      nodePort: 30007  # external port (fixed for Minikube access)
